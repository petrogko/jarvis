# Development + CI dependencies, separate from runtime requirements.txt.
# Install with:  pip install -r requirements-dev.txt
#
# Pinned for reproducibility; pip-audit any bump.
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
pip-audit==2.10.0
